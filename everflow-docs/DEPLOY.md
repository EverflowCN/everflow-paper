# 部署到 GitHub Pages

1. 将 `site/` 目录中的内容放到用于 Pages 的分支或 `/docs` 发布目录。
2. GitHub 仓库 → Settings → Pages → 选择对应分支和目录。
3. 本站资源使用相对路径，可部署在 `username.github.io/repository/` 子路径。
4. `docs/` 是 Markdown 源文件，修改后可重新生成静态页面。
