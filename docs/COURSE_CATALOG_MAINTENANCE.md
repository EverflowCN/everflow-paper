# 408 课程目录维护规则

更新时间：2026-08-28

## 1. 内置真题与强化课程必须分离

- `site/data/oxygen.json`：只保存“历年 408 真题讲解”目录。
- `site/data/oxygen-reinforcement.json`：只保存“27考研408四科强化”内置基线目录。
- 真题自动同步不得覆盖、重写或清空强化课文件。
- 强化课同步不得写入真题文件。

这两个文件承担不同职责，任何“为了复用一个课程文件而整体替换”的修改都视为高风险变更。

## 2. 四科强化的当前完整基线

当前已确认的最低完整课时数：

- DS：37
- CO：29
- OS：29
- CN：28
- 合计：123

CN 的完整基线必须至少包含到：

- 路由器与 SDN 专题
- 路由协议专题
- 网络层选择题专题（上/下）
- 网络层综合大题专题
- TCP 首部与 TCP 连接专题
- TCP 流量控制与拥塞控制专题
- 传输层选择题专题
- DHCP 与 DNS 协议专题
- 传输层综合大题专题
- FTP 协议与电子邮件系统专题
- HTTP 协议专题
- 应用层选择题专题

只显示到“分组转发流程与 ARP 协议专题”说明 CN 被截断，不能视为同步成功。

## 3. 静态基线与 Supabase 的关系

`oxygen-reinforcement.json` 是内置强化课的保底基线；Supabase `course_catalog_items` 是后台可管理的云目录。

规则：

1. 云端可以补充、更新课程元数据，也可以增加自定义课程。
2. 云端数据不得让内置强化课少于静态基线。
3. 前端必须采用“静态基线 + 云端增量合并”，不能用一份不完整云快照整体覆盖基线。
4. 如果 Supabase 内置目录少于静态基线，应先修复云端，再发布；前端基线守卫仍应保留作为第二道保险。
5. 后台的删除/停用操作不得级联删除历史用户进度。

当前前端守卫：`site/assets/js/course-catalog-baseline.js`。

## 4. 用户进度是独立数据，禁止跟目录一起删除

- 用户完成状态和笔记存放在 `course_states`。
- `progress_id` 一旦用于用户进度，不允许因为改标题、改排序、改 URL 而重新生成。
- Bilibili 课程优先使用稳定 `bvid:<BVID>` 作为 `item_id` 与 `progress_id`。
- 删除课程默认只做停用/归档，不物理删除已有进度。
- 恢复课程时应继续使用原来的 `progress_id`。

## 5. 新增氧气课程目录

### `oxygen-daily-question`

- 页面名称：`氧气每日一题（已完结）`
- 来源：就是氧气11的“零壹408每日一题”视频。
- 当前真实视频数：**63**。
- 时间范围：day1 到最终 `day82&83`。
- 不是“83 个独立视频”：源视频存在跳号和合并讲解，例如 day60&61、day76&77、day79&80、day82&83。
- 状态：`completed`，已完结。
- 每个条目使用 `bvid:<BVID>` 稳定 ID，不能按 day 编号重新生成 ID。

### `oxygen-code-camp`

- 页面名称：`27代码打卡营（更新中）`
- Bilibili UGC section：`27代码打卡营`
- section id：`9166773`
- 当前真实课时数：**36**（包含使用指南）。
- 当前已更新到第十二周习题-1；此数量是当前快照，不是永久上限。
- 状态：`updating`，持续更新。
- 同样使用 `bvid:<BVID>` 稳定 ID；后续新增课时只追加/更新，不重编号旧条目。

这两套课程是**动态云目录**，由 Supabase `course_catalogs` / `course_catalog_items` 作为 canonical source。它们不复制进四科强化静态基线，避免持续更新课程形成“GitHub 静态快照 + Supabase 云目录”两个相互漂移的真源。云端暂时不可用时，已有本地学习进度不能被清空；恢复连接后应继续使用原 `progress_id`。

## 6. 发布前必须检查的课程完整性

每次涉及 `/408/`、课程后台、课程同步、Supabase 迁移或课程 JSON 的修改，至少检查：

1. 真题目录和强化目录是否仍是两个独立文件。
2. 强化课数量是否不低于 DS 37 / CO 29 / OS 29 / CN 28。
3. CN 是否仍包含“应用层选择题专题”。
4. Supabase `oxygen-reinforcement` 四科数量是否与静态基线一致或更多。
5. `oxygen-daily-question` 已完结目录当前必须保持 63 个真实 BVID 条目，不得把 day 编号误当视频数量。
6. `oxygen-code-camp` 不得比上一次已确认的源 section 数量减少；当前基线为 36，后续只能随源新增/修正，除非源明确删除且人工确认。
7. `course_states` 总量及已有笔记数量是否在目录维护过程中保持不变。
8. `/408/` 中各课程目录能切换，课程数量正确。
9. 勾选课程、写备注不会触发 `location.reload()` 或整页跳转。
10. GitHub Pages 最新 workflow 必须成功后才能认为自动验证完成；真实设备交互结果单独记录在 `docs/FIX_LOG.md`。

## 7. CN 回退事故的根因

2026-08-28 排查发现：

- 静态 `oxygen-reinforcement.json` 中 CN 实际有 28 节，是完整的。
- Supabase `oxygen-reinforcement` 中当时只有 CN 前 14 节。
- `/408/` 使用云目录优先，因此不完整的 14 节云快照覆盖了完整静态目录，页面看起来像“CN 回退”。

修复方式：

- 补回 Supabase 缺失的 CN 第 15—28 节；
- 保持原有 `progress_id`，不改用户进度；
- 增加静态基线合并守卫，防止不完整云目录再次把内置课缩短；
- Pages 发布校验增加四科最低完整数量和 CN 末尾课时检查。

## 8. 后续自动同步约束

“408 Course Sync”可以维护历年真题与仍在更新的代码打卡营，但两者必须按 catalog 分开同步：

- 真题只写 `oxygen-past-papers`；
- 代码打卡营只写 `oxygen-code-camp`；
- 每日一题已完结，默认只做完整性检查，不应每天重写；
- 不修改 `oxygen-reinforcement` 静态基线；
- 不删除或重写 `course_states`；
- 没有真实源变化就不提交/通知；
- 代码打卡营同步必须采用 BVID upsert + 软归档确认机制，不得用“整表删除后重建”。

## 一句话维护原则

**课程目录可以新增和修正，但内置课程不能因为云同步、迁移或重构而变少；动态课程用稳定 BVID 增量维护；目录和用户进度永远分离。**
